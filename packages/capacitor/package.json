{
  "name": "webview",
  "version": "1.0.0",
  "description": "webview android/ios",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sync": "npx cap sync",
    "run": "npx cap run",
    "dev": "vite --host",
    "build": "vite build && npx cap sync",
    "serve": "vite preview",
    "generate:splash": "cordova-res android --skip-config --copy"
  },
  "keywords": [
    "android",
    "ios"
  ],
  "author": "s.deletang@icloud.com",
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^3.1.2",
    "@capacitor/core": "^3.1.2",
    "vite": "^2.4.4"
  },
  "devDependencies": {
    "@capacitor/cli": "^3.1.2"
  }
}
